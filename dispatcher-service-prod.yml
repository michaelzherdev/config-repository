spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
    consumer:
      properties:
        schema.registry.url: ${SCHEMA_REGISTRY_URL}
    producer:
      properties:
        schema.registry.url: ${SCHEMA_REGISTRY_URL}